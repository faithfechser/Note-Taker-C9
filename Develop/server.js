// Import Modules
const express = require('express');
const path = require('path');
const fs = require('fs');
// Express Application
const app = express();
// PORT
const PORT = process.env.PORT || 3000;